import React from "react";
import Hero from "../components/Hero/Hero";

const HomePage = () => {
  return (
    <div>
      <Hero/>
    </div>
  );
};

export default HomePage;
//so in donation app what wee need to make next page? 